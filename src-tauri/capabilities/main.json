{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Permite que la ventana principal reciba los eventos emitidos por el motor de Kuali.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen"]
}
